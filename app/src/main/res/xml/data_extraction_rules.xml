<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data extraction rules. Mirrors `backup_rules.xml` because
  the calculator stores no PII; both cloud backup and device-to-device
  transfer include the same data.

  See: https://developer.android.com/about/versions/12/backup-restore
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
