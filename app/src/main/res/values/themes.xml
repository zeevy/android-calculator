<?xml version="1.0" encoding="utf-8"?>
<!--
  XML themes are intentionally minimal: the real theming happens in
  Compose (`core/designsystem/theme/Theme.kt`). These XML themes only
  need to:
    - Provide a `Theme.<App>.Starting` for the SplashScreen API.
    - Set window flags before Compose attaches.

  Keeping the XML theme thin avoids drift between an XML theme palette
  and the Compose ColorScheme - the Compose layer is the source of truth.
-->
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash screen theme. Inherits from the platform Material SplashScreen.
         Background is the calculator's black canvas so the splash hands
         off into the running app without a colour flash. -->
    <style name="Theme.Calculator.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Calculator</item>
    </style>

    <!-- App theme after splash hands off. Compose owns colors/typography.
         windowLightStatusBar=false because the app's background is dark;
         the system status-bar icons need to render light against it. -->
    <style name="Theme.Calculator" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    </style>

    <!-- Floating calculator button styles. iOS palette mirroring the
         in-app keypad so the overlay reads as the same calculator. -->
    <style name="FloatingKeyBase" parent="android:Widget.Material.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="FloatingKeyDigit" parent="FloatingKeyBase">
        <item name="android:background">@drawable/floating_key_digit</item>
    </style>

    <style name="FloatingKeyOp" parent="FloatingKeyBase">
        <item name="android:background">@drawable/floating_key_op</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="FloatingKeyModifier" parent="FloatingKeyBase">
        <item name="android:background">@drawable/floating_key_modifier</item>
        <item name="android:textColor">#000000</item>
    </style>

</resources>
